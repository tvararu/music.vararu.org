# music.vararu.org

Install correct version of `node` using `nvm`:

```bash
nvm <.nvmrc
```

Install dependencies:

```bash
yarn
```

Run locally:

```bash
yarn dev
open http://localhost:3000
```

Deploy:

```bash
yarn deploy
```

## License

[MIT](LICENSE.txt).
